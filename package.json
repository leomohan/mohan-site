// package.json (CORRECTED)
{
  "name": "mohan-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tinacms dev -c \"npx http-server .\"", // CORRECTED: Double quotes around `npx http-server .` are escaped.
    "build": "node build-content.js && tinacms build",
    "start": "npx http-server ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tinacms": "^1.x.x",
    "@tinacms/cli": "^1.x.x",
    "graphql": "^16.x.x",
    "graphql-request": "^6.x.x",
    "http-server": "^14.x.x"
    // "@tinacms/mdx": "^1.x.x" // Uncomment if you add rich-text to HTML conversion
  }
}